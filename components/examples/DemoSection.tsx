import { DemoSection } from '../demo-section';
import { ThemeProvider } from '../theme-provider';

export default function DemoSectionExample() {
  return (
    <ThemeProvider>
      <DemoSection />
    </ThemeProvider>
  );
}
